---
name: sb:bases
description: (Re)generate Obsidian Bases database views over the vault — Lessons, Projects, Conversations, Tasks, Memory. Idempotent; writes .base files into 00_Dashboard.
---

# /sb:bases

```bash
node ~/.claude/skills/sb/commands/_runners/bases.js
```

Show output verbatim. Generates 5 `.base` files in `00_Dashboard/`. Open them in Obsidian
(Bases core plugin) to browse notes as filterable tables. Safe to re-run anytime; it is also
regenerated by `/sb:consolidate`.
