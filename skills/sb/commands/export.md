---
name: sb:export
description: Bundle a topic + all its tagged + wikilinked notes into a single markdown file under `exports/` for sharing. Usage — `/sb:export <topic-slug>` or `/sb:export "#area/sub"`.
---

# /sb:export

```bash
node ~/.claude/skills/sb/commands/_runners/export.js $ARGUMENTS
```
