---
name: code_assist:domains
description: Domain playbooks - condensed backend/frontend/data/animation-3d/tui guidance. Triggers on 'design a service', 'build a landing page', 'supabase', 'three.js', 'build a TUI' (routes to the right playbook).
---

# /code_assist:domains

Read `~/.claude/skills/code_assist/domains/ROUTER.md` and follow it.
