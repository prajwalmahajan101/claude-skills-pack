---
name: code_assist:structure
description: Audit a repo against the canonical project structure and report gaps with a compliance score. Triggers on 'project structure', 'is this repo set up right', 'structure audit', 'scaffold a project'.
---

# /code_assist:structure

Read `~/.claude/skills/code_assist/structure/ROUTER.md` and follow it.
