---
name: code_assist:release
description: Cut a release - CHANGELOG + version bump + annotated tag (+ optional GitHub release). Triggers on 'release', 'cut a release', 'bump version', 'tag a release'.
---

# /code_assist:release

Read `~/.claude/skills/code_assist/release/ROUTER.md` and follow it.
