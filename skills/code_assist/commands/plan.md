---
name: code_assist:plan
description: Plan work with discipline — brainstorm the design, write a reviewable plan, then execute it. The HARD-GATE before design-dependent code. Triggers on "plan this", "brainstorm", "design approach", "break this down".
---

# /code_assist:plan

Read `~/.claude/skills/code_assist/plan/ROUTER.md` and follow it. With no clear design,
start at `brainstorm.md`; then `write.md` (get approval) before `execute.md`.
