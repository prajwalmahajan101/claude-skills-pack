---
name: code_assist:flow
description: Cross-family orchestrator — run a curated chain (ship, start, fix, land) that threads review, commit, journal, track, notify, plan, structure together. Triggers on "ship this", "take this from ticket to PR", "full flow".
---

# /code_assist:flow

Read `~/.claude/skills/code_assist/orchestrator/ROUTER.md` and follow the requested chain.
