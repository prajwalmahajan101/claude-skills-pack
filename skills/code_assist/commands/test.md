---
name: code_assist:test
description: Test-driven development - write the failing test first, then the code. Also add regression tests / fill coverage. Triggers on 'TDD', 'write tests', 'test first', 'add a regression test'.
---

# /code_assist:test

Read `~/.claude/skills/code_assist/test/ROUTER.md` and follow it.
