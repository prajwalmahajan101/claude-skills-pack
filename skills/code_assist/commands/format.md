---
name: code_assist:format
description: Format markdown (zero-dep, code-block-safe) or code via the project formatter. Triggers on 'format', 'md format', 'clean up formatting', 'prettier'.
---

# /code_assist:format

Read `~/.claude/skills/code_assist/format/ROUTER.md` and follow it.
