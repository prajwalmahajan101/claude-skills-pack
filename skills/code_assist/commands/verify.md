---
name: code_assist:verify
description: Verification-before-completion — produce fresh evidence (run this turn) that the change meets its goal before claiming done. Triggers on "verify", "confirm it works", "is this actually done".
---

# /code_assist:verify

Read `~/.claude/skills/code_assist/verify/ROUTER.md` and follow it.
