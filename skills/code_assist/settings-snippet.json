{
  "_comment": "Merge these into <CLAUDE_DIR>/settings.json under 'hooks' (default CLAUDE_DIR=~/.claude). install.sh substitutes $CLAUDE_DIR and merges idempotently with jq. Hooks live under the symlinked skill dir; both honor CA_DISABLE=1, git-guard hard-blocks only under CA_GIT_GUARD_STRICT=1.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          { "type": "command", "command": "node \"$CLAUDE_DIR/skills/code_assist/hooks/ca-session-start.js\"", "timeout": 5 }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          { "type": "command", "command": "node \"$CLAUDE_DIR/skills/code_assist/hooks/ca-git-guard.js\"", "timeout": 5 }
        ]
      }
    ]
  }
}
