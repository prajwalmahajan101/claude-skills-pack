# CLAUDE.md

> Guidance for Claude Code (and humans) working in this repo. Keep under ~200 lines.

## What this is
One paragraph: the project's purpose and current state.

## Tech stack
- Language / framework / runtime versions.
- Key libraries and why they were chosen.

## Build / test / lint commands
- Install: `<cmd>`
- Run: `<cmd>` (or `make run`)
- Test: `<cmd>` (or `make test`)
- Lint + type check: `<cmd>` (or `make lint`)

## Conventions (not obvious from the code)
- Layering / module boundaries.
- Naming, error-handling, logging conventions.

## Known gotchas
- Anything that will surprise a newcomer.

## Architecture decisions (index)
See [docs/adr/INDEX.md](docs/adr/INDEX.md). One line per ADR; bodies live in `docs/adr/`.
